apiVersion: apps.open-cluster-management.io/v1
kind: PlacementRule
metadata:
  name: eu-region-clusters
  namespace: reverse-words-region
spec:
  clusterConditions:
   - type: OK
  clusterSelector:
    matchExpressions: []
    matchLabels:
      region: "EU"
